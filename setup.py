from setuptools import setup, find_packages

setup(
    name='basedir',
    version="1.0.0",
    description="Library that simplifies access to local directories and files",
    long_description="",
    author='kawazome',
    packages=find_packages()
)